#pragma once

enum GameStates
{
    STATE_SHIP_PLACEMENT,
    STATE_BATTLE,
    STATE_WINNER

};

enum FontsIndexes
{
    FONT_PACMAN,
};

enum BattleStates
{
    BATTLE_STATE_PLAYER,
    BATTLE_STATE_BOT
};

enum ImagesIndexes
{
    IMAGE_FIRE,
    IMAGE_CROSS,
    IMAGE_WATER,
    IMAGE_SHIP_1,
    IMAGE_SHIP_2,
    IMAGE_SHIP_3,
    IMAGE_SHIP_4
};

enum TexturesIndexes
{
    TEXTURE_FIRE,
    TEXTURE_CROSS,
    TEXTURE_WATER,
    TEXTURE_SHIP_1,
    TEXTURE_SHIP_2,
    TEXTURE_SHIP_3,
    TEXTURE_SHIP_4
};

enum Musics
{
    MUSIC_SEA
};

enum CHUNKS
{
    CHUNK_SIREN,
    CHUNK_SHOUT,
    CHUNK_SKY,
    CHUNK_EXPLOSION
};

enum CHANNELS
{
    CHANNEL_SIREN,
    CHANNEL_SHOUT,
    CHANNEL_SKY,
    CHANNEL_EXPLOSION
};