#pragma once

enum GameStates
{
    STATE_SHIP_PLACEMENT,
    STATE_BATTLE,
    STATE_WINNER

};

enum FontsIndexes
{
    FONT_PACMAN,
};

enum BattleStates
{
    BATTLE_STATE_PLAYER,
    BATTLE_STATE_BOT
};